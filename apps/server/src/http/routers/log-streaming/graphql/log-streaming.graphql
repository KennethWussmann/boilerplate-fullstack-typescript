type Subscription {
  logStream(history: Boolean = true): LogEntry!
}

type LogEntry {
  timestamp: DateTime!
  level: LogLevel!
  name: String
  message: String!
  metadata: String
}

enum LogLevel {
  FATAL
  ERROR
  WARN
  NOTICE
  INFO
  DEBUG
}
